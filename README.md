# quantum_rules_kit
Quantum loop abstraction (fixed, uniform, reversible)


If it passes these decorators, it can be embedded into a quantum circuit.

If it fails, quantum mechanics—not Qiskit—says “no.”
